# RAMO - Sistema de Facturación (Backend)

API REST desarrollada en .NET Framework 4.8 con Entity Framework para la gestión de facturas, clientes y productos.

## 🚀 Configuración Inicial
1. **Base de Datos:**
   - Ejecute el script localizado en `/Database/script_db_ramo.sql` en su instancia de SQL Server.
2. **Cadena de Conexión:**
   - Abra el archivo `Web.config`.
   - Modifique la sección `<connectionStrings>` con sus credenciales locales de SQL Server.
3. **Ejecución:**
   - Abra la solución (.sln) en Visual Studio.
   - Restaure los paquetes NuGet y presione `F5`.

## 🛠 Tecnologías
- C# / .NET Web API
- Entity Framework (Database First)
- SQL Server